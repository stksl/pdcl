#ifdef ignored // [0] EXPECTED: Success, not defined
#endif

// [1] EXPECTED: Success
#ifdef someArgedMacro
#endif

// [2] EXPECTED: EOF exception (no endif)
#ifndef someArgedMacro
    #ifdef someArgedMacro
    #endif
